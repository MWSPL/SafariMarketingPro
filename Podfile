# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SafariMarketingPro' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SafariMarketingPro
  pod 'SlideMenuControllerSwift'
  pod 'Tamamushi'
  pod 'NVActivityIndicatorView'
  pod 'SwiftValidator'
  pod 'ASToast', '~> 1.1.2'
  pod 'Alamofire', '~> 4.7'
  target 'SafariMarketingProTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SafariMarketingProUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
